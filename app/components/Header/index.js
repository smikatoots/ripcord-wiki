import React from 'react';
import PropTypes from 'prop-types';

const Header = props => {

  return (
    <div className="Header">
      Header
    </div>
  );
};

Header.defaultProps = {
};

Header.propTypes = {
};

export default Header;
